<form id="searchUserForm" action="/searchUsers" method="post">
    <div class="centerLogin" style="margin-top:10px">
        <input type="text" id="searchInput" name="username" placeholder="Enter username">
        <button class="homeButton" type="submit" style="margin-top:10px">Search</button>
    </div>
</form>


<!-- Display search results -->
<div id="searchResults" class="centerLogin">

    {{#if users}}
    <h2>Search Results:</h2>
        {{#each users}}
            <p>User found: {{this.userName}}</p>
            <form class="sendFriendRequestForm" action="/addFriend" method="post">
                <input type="hidden" name="username" value="{{this.userName}}">
                <button type="submit" id="addFriendButton" class="homeButton">Add as Friend</button>
            </form>
        {{/each}}
    {{/if}}
</div>
<div class="container">
    <button class="homeButton" onclick="location.href='/protected';" type="submit">Home Page</button>
</div>

<script src="/public/js/client_side_addUser.js"></script>